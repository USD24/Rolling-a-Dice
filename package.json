{
  "name": "dice-project",
  "version": "1.0.0",
  "description": "The project created with NodeJs and ExpressJs, that randomly displays two dice values and also displays its sum.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Uddesh SD",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
